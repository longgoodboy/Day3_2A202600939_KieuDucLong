{
  "description": "Strict tool descriptions used by the implemented registry.",
  "tools": [
    {
      "name": "search_products",
      "description": "Search the local product catalog by optional query, category, and max_price in VND. Returns sorted matching products or an empty result."
    },
    {
      "name": "get_product_details",
      "description": "Get product ID, normalized product name, category, unit price in VND, stock, and package weight in kilograms. Required argument: item_name."
    },
    {
      "name": "check_stock",
      "description": "Check available quantity for a product and whether requested_quantity can be fulfilled. Required: item_name. Optional: requested_quantity positive integer."
    },
    {
      "name": "get_discount",
      "description": "Check coupon status. Returns active, expired, or unknown status and only returns a positive discount for active coupons. Required: coupon_code."
    },
    {
      "name": "calc_shipping",
      "description": "Calculate shipping fee in VND using positive weight_kg and supported Vietnamese destination. Returns structured error for unsupported cities."
    },
    {
      "name": "calculate_order_total",
      "description": "Calculate subtotal, discount amount, shipping fee, and final total in VND. Requires unit_price, quantity, discount_percent, and shipping_fee."
    }
  ]
}
